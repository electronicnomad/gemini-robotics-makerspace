Flask==3.0.0
opencv-python==4.8.1.78
# For the local follow-tracker: opencv-python provides TrackerMIL (fallback). Install
# opencv-contrib-python instead of opencv-python for better/faster CSRT/KCF trackers.
# opencv-contrib-python==4.8.1.78
numpy<2.0.0
# google-genai must be a Live-API-capable version (client.aio.live). If the robot logs
# "no Live API support", run: pip install -U google-genai
google-genai==2.10.0
python-dotenv==1.0.1
pyserial==3.5
# vosk no longer required: voice commands are recognized by Gemini audio (pyaudio still needed for mic capture).
# vosk==0.3.45
pyaudio==0.2.14
Pillow==10.0.1
setuptools
pydantic>=2.0.0
